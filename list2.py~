"""write a program that returns a list that contains only the elements that are common between the lists (without duplicates). Make sure your program works on two lists of different sizes."""

l1=[]
l2=[]
l1_n=input("enter the number of elements in list 1:")
for i in range(int(l1_n)):
	l1.append(int(input("enter data:")))
l2_n=input("enter the number of elements in list 2:")
for i in range(int(l2_n)):
	l2.append(int(input("enter data:")))
l=list(set(l1)&set(l2))
print(l)

